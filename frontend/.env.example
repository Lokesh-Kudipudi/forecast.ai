# Base URL of the FastAPI backend. Required.
VITE_API_BASE_URL=http://localhost:8000

# Environment label shown in the navbar pill (production/staging).
VITE_APP_ENV=staging

# Optional shared bearer token (unset by default).
# VITE_API_TOKEN=
